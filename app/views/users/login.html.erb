<h1>User Login</h1>


<%= form_for(@user, url: login_path, method: 'post') do |f| %>

    <div>
      <%= f.label :email%>
      <%= f.text_field :email %>
    </div>

    <div> 
      <%= f.label :password , "password"%>
      <%= f.password_field :password %>     
    </div>

    <div>
      <%= f.submit "Login"%>
    </div>
    
<%end%>
