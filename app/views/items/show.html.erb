<h1><%= @item.name %></h1>

<ul>
  <li>價錢：<%= @item.price %></li>
  <li>種類：<%= @category %></li>
</ul>

<h3>留言</h3>
<%= form_for(@comment, url: item_comments_path(@item)) do |form| %>

  <%# <%= form.label :content %> 
  
  <div>
    <%= form.text_area :content%>
  </div>

  <%= form.submit%>
<% end %>

